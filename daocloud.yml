version: 2.0
test:
build:
    lite_image:
        compile:
            dockerfile_path: Dockerfile
            build_dir: /
            cache: true
        extract:
            - /go/bin/myblog
            - /go/src/myblog/conf
            - /go/src/myblog/static
            - /go/src/myblog/views
        package:
            dockerfile_path: Dockerfile.package
            build_dir: /
            cache: true