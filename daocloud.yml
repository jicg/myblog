version: 2.0
test:
build:
    lite_image:
        compile:
            dockerfile_path: Dockerfile
            build_dir: /
            cache: true
        extract:
            - /go/bin/myblog
            - /go/src/github.com/jicg/myblog/conf
            - /go/src/github.com/jicg/myblog/static
            - /go/src/github.com/jicg/myblog/views
        package:
            dockerfile_path: Dockerfile.package
            build_dir: /
            cache: true